# 🌐 刷题系统 - 免费云平台部署指南

## 📋 目录
1. [Render.com部署（推荐）](#1-rendercom部署推荐)
2. [Railway.app部署](#2-railwayapp部署)
3. [PythonAnywhere部署](#3-pythonanywhere部署)
4. [Fly.io部署](#4-flyio部署)
5. [常见问题](#5-常见问题)

---

## 1️⃣ Render.com部署（最推荐） ⭐⭐⭐⭐⭐

### 🎁 免费额度
- **Web服务**：750小时/月（足够个人使用）
- **PostgreSQL数据库**：1GB存储，免费
- **带宽**：100GB/月
- **自定义域名**：支持
- **HTTPS**：自动配置

### 📝 部署步骤

#### 步骤1：上传代码到GitHub

1. 访问 [GitHub](https://github.com) 并登录
2. 点击右上角 `+` → `New repository`
3. 填写仓库信息：
   - Repository name: `quiz-system`（或你喜欢的名字）
   - 选择 `Public`
   - 勾选 `Add a README file`
4. 点击 `Create repository`

5. 在本地电脑打开Git Bash，执行：

```bash
# 进入项目目录
cd D:/shuati/quiz_system

# 初始化Git
git init

# 添加所有文件
git add .

# 提交
git commit -m "初始提交：智能刷题系统"

# 连接远程仓库（替换成你的仓库地址）
git remote add origin https://github.com/你的用户名/quiz-system.git

# 推送到GitHub
git push -u origin main
```

#### 步骤2：在Render上部署

1. 访问 [Render.com](https://render.com) 并注册/登录
   - 推荐用GitHub账号登录（更简单）

2. 点击 `New +` → `Web Service`

3. 连接GitHub仓库：
   - 选择 `Connect a repository`
   - 授权Render访问你的GitHub
   - 选择 `quiz-system` 仓库

4. 配置Web服务：
   - **Name**: `quiz-system`（或你喜欢的名字）
   - **Region**: 选择 `Oregon (US West)` 或 `Frankfurt (EU Central)`
   - **Branch**: `main`
   - **Runtime**: `Python 3`
   - **Build Command**: 
     ```
     pip install -r requirements.txt
     ```
   - **Start Command**: 
     ```
     gunicorn app:app
     ```

5. 创建数据库：
   - 在同一页面，找到 `Environment Variables` 部分
   - Render会自动创建 `DATABASE_URL` 环境变量（如果你用了`render.yaml`）

   或者手动创建PostgreSQL数据库：
   - 点击 `New +` → `PostgreSQL`
   - **Name**: `quiz-db`
   - **Database**: `quiz`
   - **User**: 保持默认
   - **Plan**: `Free`
   - 点击 `Create Database`
   - 创建完成后，复制 `Internal Database URL`
   - 回到Web服务设置，添加环境变量：
     - Key: `DATABASE_URL`
     - Value: 粘贴刚才复制的URL

6. 添加其他环境变量：
   - Key: `SECRET_KEY`
   - Value: 点击 `Generate` 生成一个随机密钥

7. 点击 `Create Web Service`

8. 等待部署完成（约5-10分钟）

9. 部署成功后，你会得到一个URL：
   ```
   https://quiz-system-xxxx.onrender.com
   ```

10. 访问该URL，使用默认管理员账号登录：
    - 用户名: `admin`
    - 密码: `admin123`

---

## 2️⃣ Railway.app部署 ⭐⭐⭐⭐

### 🎁 免费额度
- **$5/月免费额度**（足够运行小应用）
- **支持MySQL、PostgreSQL、MongoDB**
- **自动HTTPS**

### 📝 部署步骤

1. 访问 [Railway.app](https://railway.app) 并注册/登录
   - 推荐用GitHub账号登录

2. 点击 `New Project` → `Deploy from GitHub repo`

3. 选择 `quiz-system` 仓库

4. 添加数据库：
   - 点击 `New` → `Database`
   - 选择 `PostgreSQL` 或 `MySQL`
   - Railway会自动添加 `DATABASE_URL` 环境变量

5. 配置环境变量：
   - 点击你的Web服务
   - 进入 `Variables` 标签
   - 添加：
     - `SECRET_KEY`: 任意随机字符串
     - `PYTHON_VERSION`: `3.11`

6. 部署会自动开始
   - 等待约5分钟

7. 部署完成后，点击 `Settings` → `Domains`
   - Railway会自动生成一个域名
   - 或者你可以添加自定义域名

8. 访问生成的URL，开始使用

---

## 3️⃣ PythonAnywhere部署 ⭐⭐⭐

### 🎁 免费额度
- **永久免费**（不休眠）
- **MySQL数据库**：免费
- **存储空间**：500MB
- **带宽**：有限制

### 📝 部署步骤

1. 访问 [PythonAnywhere.com](https://www.pythonanywhere.com) 并注册免费账号

2. 登录后，点击 `Consoles` → `Bash`

3. 克隆GitHub仓库：

```bash
cd ~
git clone https://github.com/你的用户名/quiz-system.git
cd quiz-system
```

4. 创建虚拟环境并安装依赖：

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

5. 配置数据库：
   - 点击 `Databases` 标签
   - 创建MySQL数据库（免费）
   - 记录数据库连接信息

6. 修改 `app.py`，使用MySQL数据库：

在 `app.py` 开头添加：

```python
import os

# PythonAnywhere数据库连接
if 'PYTHONANYWHERE_DOMAIN' in os.environ:
    # 生产环境（PythonAnywhere）
    db_user = '你的用户名'
    db_password = '你的数据库密码'
    db_name = '你的用户名$quiz'
    app.config['SQLALCHEMY_DATABASE_URI'] = f'mysql+pymysql://{db_user}:{db_password}@mysql.server/{db_name}'
else:
    # 本地开发
    app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///quiz.db'
```

7. 创建WSGI配置文件：
   - 点击 `Web` 标签
   - 点击 `Add a new web app`
   - 选择 `Flask`
   - **Python file**: `/home/你的用户名/quiz-system/app.py`
   - **Virtualenv**: `/home/你的用户名/quiz-system/venv`

8. 点击 `Reload` 按钮

9. 访问你的网站：
   ```
   https://你的用户名.pythonanywhere.com
   ```

---

## 4️⃣ Fly.io部署 ⭐⭐⭐⭐

### 🎁 免费额度
- **3个小应用**（256MB内存）
- **3GB存储空间**
- **160GB带宽/月**

### 📝 部署步骤

1. 安装Fly CLI：
   - Windows:
     ```powershell
     iwr https://fly.io/install.ps1 -useb | iex
     ```
   - 或者下载安装包

2. 登录Fly：
```bash
fly auth login
```

3. 在项目目录初始化：
```bash
cd D:/shuati/quiz_system
fly launch
```

4. 按照提示操作：
   - 选择 `y` 部署
   - 选择区域（推荐 `sin` 新加坡）
   - 选择 `n` 不创建PostgreSQL（免费额度不够）
   - 使用SQLite数据库

5. 部署完成：
```bash
fly deploy
```

6. 访问生成的URL

---

## 5️⃣ 常见问题

### ❓ 部署后无法访问

**可能原因**：
1. 环境变量未配置
2. 数据库未创建
3. 代码有bug

**解决方法**：
- 查看部署日志
- 确认所有环境变量已设置
- 手动创建数据库表

在Render/Railway中，你可以添加启动命令来初始化数据库：

```bash
# 在Build Command中添加
pip install -r requirements.txt && python init_db.py
```

创建 `init_db.py`：

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
from app import app, db

with app.app_context():
    db.create_all()
    
    # 创建默认管理员
    from app import User
    admin = User.query.filter_by(username='admin').first()
    if not admin:
        admin = User(username='admin', role='admin')
        admin.set_password('admin123')
        db.session.add(admin)
        db.session.commit()
        print('✅ 默认管理员创建成功')
    else:
        print('ℹ️ 管理员已存在')
    
    print('✅ 数据库初始化完成')
```

### ❓ 数据库迁移

如果需要修改数据模型，可以使用Flask-Migrate：

```bash
pip install flask-migrate
```

在 `app.py` 中添加：

```python
from flask_migrate import Migrate
migrate = Migrate(app, db)
```

然后执行：

```bash
flask db init
flask db migrate -m "Initial migration"
flask db upgrade
```

### ❓ 静态文件无法加载

**解决方法**：
- 确保 `static` 目录存在
- 在模板中使用 `url_for('static', filename='xxx.css')`

### ❓ 免费额度用完了怎么办

**Render.com**：
- 750小时/月 = 约31天
- 如果超出，服务会暂停，下个月自动恢复
- 或者升级到付费计划（$7/月）

**Railway.app**：
- $5额度用完后，服务会暂停
- 添加信用卡可继续使用（按量付费）

---

## 🎯 推荐方案总结

| 需求 | 推荐平台 |
|------|----------|
| 完全免费、稳定 | **Render.com** |
| 简单易用 | **Railway.app** |
| 永久免费 | **PythonAnywhere** |
| 高性能 | **Fly.io** |

---

## 📞 需要帮助？

如果遇到问题，可以：
1. 查看平台提供的部署日志
2. 检查环境变量是否正确
3. 确认数据库是否创建
4. 向我提问（提供错误日志）

祝部署顺利！🎉
